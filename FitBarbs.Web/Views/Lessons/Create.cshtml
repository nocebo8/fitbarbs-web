@model FitBarbs.Web.Models.Lesson

<h2>Nowa lekcja</h2>

<form asp-action="Create" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="CourseId" />
    <div class="mb-3">
        <label class="form-label">Tytuł</label>
        <input asp-for="Title" class="form-control" />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label class="form-label">Opis</label>
        <textarea asp-for="Description" class="form-control"></textarea>
    </div>
    <div class="mb-3">
        <label class="form-label">Wideo (mp4)</label>
        <input type="file" name="video" accept="video/*" class="form-control" />
        <span asp-validation-for="VideoPath" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary">Zapisz</button>
    <a href="@Url.Action("Details", "Courses", new { id = Model.CourseId })" class="btn btn-secondary">Anuluj</a>
</form>

<partial name="_ValidationScriptsPartial" />


